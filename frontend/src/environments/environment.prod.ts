export const environment = {
  production: true,
  // IMPORTANT: Replace the placeholder below with your real Stripe publishable key
  // during your CI/CD pipeline or deployment process. Do NOT commit your live keys.
  stripePublishableKey: 'pk_live_REPLACE_ME'
};
